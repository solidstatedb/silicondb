name: build-and-test
on: [push, pull_request]
jobs:
  build-and-test-using-cmake:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cmake --version
      - run: cmake -S . -B build/
      - run: cd build && ctest
